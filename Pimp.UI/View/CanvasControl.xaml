<UserControl x:Class="Pimp.View.CanvasControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModel="clr-namespace:Pimp.ViewModel"
             xmlns:ui="clr-namespace:Pimp.UI"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             Focusable="True"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <viewModel:CanvasViewModel/>
    </UserControl.DataContext>

    <Canvas x:Name="canvas" Width="10000" Height="10000" Background="#3A3A3A" 
            MouseRightButtonDown="MouseRightButtonDown_Canvas">
        <ItemsControl ItemsSource="{Binding GridLines}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Path Stroke="{Binding Path=GridBrush}" StrokeThickness="1">
                        <Path.Data>
                            <LineGeometry StartPoint="{Binding StartPoint}" EndPoint="{Binding EndPoint}" />
                        </Path.Data>
                    </Path>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <StackPanel x:Name="MethodListStackPanel" Orientation="Vertical" Visibility="Collapsed" Panel.ZIndex="100">
            <TextBox x:Name="SearchBox" Width="200" Height="30" TextChanged="SearchBox_TextChanged"/>
            <ListBox x:Name="MethodListBox" 
                     Width="200"
                     Height="400"
                     ItemsSource="{Binding ProcessingUnitMethods}">
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding MethodDoubleClickCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Name}">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Visibility}" Value="Collapsed">
                                            <Setter Property="Visibility" Value="Collapsed"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>

        <ItemsControl x:Name="itemsControl" 
                      ItemsSource="{Binding CanvasMethodInfoInstances}"
                      ItemTemplate="{StaticResource CanvasMethodInstanceTemplate}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style>
                    <Setter Property="Canvas.Left" Value="{Binding CanvasPos.X}"/>
                    <Setter Property="Canvas.Top" Value="{Binding CanvasPos.Y}"/>
                </Style>
            </ItemsControl.ItemContainerStyle>
        </ItemsControl>

        <i:Interaction.Triggers>
            <i:EventTrigger EventName="MouseLeftButtonDown">
                <i:InvokeCommandAction Command="{Binding LeftButtonDownCommand_MethodInstance}" PassEventArgsToCommand="True"/>
            </i:EventTrigger>
            <i:EventTrigger EventName="MouseLeftButtonUp">
                <i:InvokeCommandAction Command="{Binding LeftButtonUpCommand_MethodInstance}" PassEventArgsToCommand="True"/>
            </i:EventTrigger>
        </i:Interaction.Triggers>
    </Canvas>
</UserControl>
